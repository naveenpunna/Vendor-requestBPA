_schema-version: "3.1"
ID: Vendor_request_annoel
description: A simple CAP project.
version: 1.0.0
modules:
- name: Vendor_request_annoel-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Vendor_request_annoel-db
  - name: Vendor_request_annoel-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: Vendor_request_annoel-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Vendor_request_annoel-db
  parameters:
    buildpack: nodejs_buildpack
- name: Vendor_request_annoel-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Vendor_request_annoel-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - commindsetvendorrequestcapapplication.zip
      name: commindsetvendorrequestcapapplication
      target-path: resources/
- name: commindsetvendorrequestcapapplication
  type: html5
  path: app/vendor_request_cap_application
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Vendor_request_annoel-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Vendor_request_annoel-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: Vendor_request_annoel
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: Vendor_request_annoel-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Vendor_request_annoel-html5-srv
    service-plan: app-host
- name: Vendor_request_annoel-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Vendor_request_annoel-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
